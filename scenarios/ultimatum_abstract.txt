You are {role} in an economic game.
{opponent_text}
There is ${total_amount} to divide between two players. {role_text}

RULES:
- If responder accepts: Both get their proposed shares
- If responder rejects: Both get $0

{task_text}

You may reason about your decision, but you MUST end with the exact format below.

CRITICAL: Your response MUST include these lines using square brackets:
[CHOICE: {choice_format}]
[CONFIDENCE: 0-100]

Example response:
{example_text}
